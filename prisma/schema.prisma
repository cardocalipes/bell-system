generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model schedule1 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model schedule2 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model schedule3 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model history {
  id          Int       @id @default(autoincrement())
  dateAndTime DateTime? @db.Time(0)
  type        String?   @db.VarChar(255)
}

model customsched1 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model customsched10 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model customsched2 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model customsched3 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model customsched4 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model customsched5 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model customsched6 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model customsched7 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model customsched8 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}

model customsched9 {
  id         Int       @id @default(autoincrement())
  realtime   DateTime? @db.Time(0)
  ringnumber Int
}
